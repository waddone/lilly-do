<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="description" content="Lillydoo test">
        <meta name="author" content="Adrian Vasilescu 4">
        <title>{% block title %}Welcome to address book test !{% endblock %}</title>
        {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}" />
        <link rel="stylesheet" href="{{ asset('assets/css/style.css?ver=1.01') }}" />
        <link rel="stylesheet" href="//cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css" />
        <link rel="stylesheet" href="https://cdn.datatables.net/1.10.19/css/dataTables.bootstrap.min.css" />
        {% endblock %}
    </head>
    <body>
        <header>
            <nav class="navbar">
                <div class="container">
                    <a class="navbar-brand" href="{{ path('overview') }}">Overview of all contacts.</a>
                </div>
            </nav>
        </header>

        <main role="main" class="container">
            {% block body %}
            {% endblock %}
        </main><!-- /.container -->


        {% block javascripts %}
        <script src="{{ asset('assets/js/jquery-3.3.1.min.js') }}"></script>
        <script src="{{ asset('assets/js/bootstrap.min.js') }}"></script>
        <script src="//cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>
        <script>
            $(document).ready( function () {
                $('#myTable').DataTable();

                $("#FormTable input").each(function(){
                    $("input").each(function(){
                            var inputName = '';
                            var inputVal  = '';
                            var inputId   = '';
                            var inputAll  = '';
                        $(this).on("keyup", function(e) {
                            inputName = $(this).attr("name");
                            inputVal  = $(this).val();
                            inputId   = $(this).attr("data-id");
                            inputAll  = inputId+'&'+inputName+'&'+inputVal;
                             
                            if(e.which == 13) {
                                console.log(inputAll);
                            }
                            

                            /*
                            var save      = $(this).next(".mic").attr('data-id', inputAll);
                            save.removeClass('hidden');
                            save.click(function() {
                                alert(inputName + " " + inputVal + " " + inputId);
                            });
                            $(save).click(function(){
                                alert(inputName + " " + inputVal + " " + inputId);
                            });
                            */
                        });
                        /*
                        $(this).on("blur", function() {
                            $(this).next(".mic").addClass('hidden');
                        });
                        */
                    });
                });
            });
            $(document).on('click', '.mic', function () {
                var data = $('.mic').attr('data-id');
                alert(data);
            });
        </script>
        {% endblock %}
    </body>
</html>